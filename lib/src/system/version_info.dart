library blackboard.rest.system.version.info;

import 'version.dart';

/// The [VersionInfo] class...
class VersionInfo {
  final Version learn;

  /// The [VersionInfo] constructor...
  VersionInfo (this.learn);
}
